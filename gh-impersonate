#!/usr/bin/env bash
set -euo pipefail

repo_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
binary="$repo_dir/bin/gh-impersonate"

if [[ -x "$binary" ]]; then
  exec "$binary" "$@"
fi

cd "$repo_dir"
exec go run ./cmd/gh-impersonate "$@"
