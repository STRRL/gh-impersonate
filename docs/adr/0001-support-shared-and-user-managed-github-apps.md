# Support shared and user-managed GitHub Apps

`gh-impersonate` will provide a shared GitHub App as the default fast-start path, while also supporting user-managed GitHub Apps for teams that need explicit ownership of permissions, installation scope, and audit boundaries. The shared app keeps first use simple; Bring Your Own GitHub App avoids making the shared app a mandatory trust dependency.
